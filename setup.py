from setuptools import setup, find_packages

setup(
    name = 'Seven Segments',
    version = '0.1.0',
    author = 'renai',
    description = 'A library to handle 7 segment display using micropython machine package',
    packages = find_packages(),
)